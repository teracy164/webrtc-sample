{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "lerna run start:dev --scope=server --stream",
    "start": "lerna run start --scope=server --stream",
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
